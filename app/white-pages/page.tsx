import WhitePagesPage from "@/components/pages/WhitePagesPage";

export const metadata = {
  title: "White Pages — Janitor Network",
  description: "Query the intelligence database for breach records, exposed credentials, and network actors.",
};

export default function Page() {
  return <WhitePagesPage />;
}
